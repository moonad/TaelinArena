// Subset.fm
// =========
// 
// A Subset is like a Sigma, except that the second element
// is erased. As such, it can be seen as a subset of the
// first type, restricted by the predicate expressed on the
// second type.

// Definition
// ----------

T Subset<A, B : A -> Type>
| subset(fst : A, snd : B(fst);)

.such_that.(A, B) : Type
  Subset(A, B)
